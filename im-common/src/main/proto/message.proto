syntax = "proto3";
option java_package = "com.itrjp.im.protobuf";
option java_outer_classname = "MessageModel";// 生成的外部类名，同时也是文件名

// protobuf 使用 message 管理数据
message Message {
  enum Type{
    CHAT = 0;
    IMAGE = 1;
  }
  Type type = 1;
  string data = 2;
  string from = 3;
  string to = 4;
}